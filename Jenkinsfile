node {
    
    stage('PreBuild') { // for display purposes
       println("Prebuild Stages")
    }
    stage('RunBuild') {
        println("Running Build Stages")
    }
    stage('FinalResults') {
        println("Final Results Stage")
    }
}